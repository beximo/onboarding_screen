import 'package:flutter/material.dart';

class HomeScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    // TODO: need to refectore this
    return Scaffold(
      body: const Center(
        child: Text('Home Page'),
      ),
    );
  }
}
